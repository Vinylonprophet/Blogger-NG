import { PageDisplayPipe } from './page-display.pipe';

describe('PageDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new PageDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
